spring:
  flyway:
    enabled: true

idam:
  s2s-auth:
    secret: ${S2S_SECRET_EUD_BACKEND:AAAAAAAAAAAAAAAA}
  client:
    secret: ${IDAM_CLIENT_SECRET:12345678}
    password: ${IDAM_CLIENT_PASSWORD:password}

lau:
  db:
    encryption-key: ${LAU_EUD_ENCRYPTION_KEY:ThisIsATestKeyForEncryption}
